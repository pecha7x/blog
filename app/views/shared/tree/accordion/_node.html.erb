<% if item[:link].present? %>
  <%= link_to item[:name], item[:link], class: "text-sm text-indigo-700 font-semibold hover:font-bold" %>
<% else %>
  <span class="text-sm text-gray-800 font-semibold">
    <%= item[:name] %>
  </span>
<% end %>
