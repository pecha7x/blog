<nav class="relative top-0 z-10 bg-gray-600">
  <div class="mx-auto max-w-7xl">
    <div class="relative flex h-16 items-center justify-between">
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
        <%= render 'layouts/navbar/mobile/button' %>
      </div>

      <div class="flex flex-1 items-center justify-center sm:justify-start">
        <div class="flex shrink-0 items-center">
          <%= link_to root_path do %>
            <%= image_tag("SoftwareEngineeringByAP.svg", class: "lg:h-[2.8rem] h-[2rem] w-auto mt-3", alt: "Software Engineering") %>
          <% end %>  
        </div>

        <div class="hidden w-full sm:flex justify-center">
          <div class="flex space-x-4">
            <% @navbar_sections.each do |section| %>
              <%= render 'layouts/navbar/menu_item', section:, active: false %>
            <% end %>
          </div>
        </div>
      </div>
      
      <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
        <%= render 'layouts/navbar/profile' %>
      </div>

    </div>
  </div>

  <el-disclosure id="mobile-menu" hidden class="sm:hidden [&:not([hidden])]:block">
    <%= render 'layouts/navbar/mobile/menu' %>
  </el-disclosure>
</nav>
