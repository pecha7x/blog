<div id="<%= dom_id category %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
     <%= icon_tag(category, zoom: 200, class: "bg-gray-300 p-1 rounded border border-gray-400") %>
  </div>
  <div>
    <%= category.name %>
  </div>
</div>
